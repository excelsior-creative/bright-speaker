# 2026-04-23 — Night 3 (of the routine; calendar-wise it's the fourth operating night)

## Arrival state

- On branch `claude/gracious-faraday-ZXSqO`, clean. HEAD is at
  `4971ea5` = current origin/main.
- Origin/main has PR #7 (night 2 honesty pass v2 + K-5 pivot) and
  PR #6 (bstack skill) merged on top of the night-1 baseline.
- **Five open draft PRs** stacked up since night 2:
  - PR #2 (night 2 backup attempt — pilot page + kid-safe filler on
    the pre-monorepo `src/` tree). Stale; probably worth closing.
  - PR #5 (Neon + Clerk auth — the production-auth track, PR'd per
    the mandate). Legitimately blocked on Brandon review.
  - PR #8 (night 3 attempt — design reconciliation + K-5 filler
    tuning + honesty regression test + second blog post).
  - PR #9 (another night 3 attempt — grade-banded filler profiles +
    honesty test + pauses-beat-um blog post + literacy coach
    outreach).
  - PR #10 (bstack Playwright harness).
- `brightspeaker.com` still 403s (INBOX #1 — fourth night open).
- `pnpm install` clean. `tsc --noEmit` clean. `pnpm run test` 2/2.
  `pnpm run lint` — 1 pre-existing error on `speak/page.tsx:304`
  (PRs #8 and #9 both fix it independently).
- Journal ended at 2026-04-20; today is 2026-04-23 so the actual
  night cadence is 3 days slower than the journal implies. Brandon
  hasn't been merging, and the last two "Night 3" branches (PR #8
  and PR #9) both sit unreviewed.

## Tonight's strategic read

Rotating sharply toward GTM tonight. Recent work has been heavy on
code and content; the pipeline remains empty at zero. The biggest
conversion gap in the GTM stack is that the Title I principal
outreach draft (and everyone who clicks through on anything
titled "free 30-day pilot") lands on a homepage, not a pilot
landing page. We also have only three outreach drafts for three
personas; the fourth persona (enrichment directors at independent
K–5 schools — our fastest signing lane) has no draft yet.

Also, five unmerged PRs is a problem. Every additional night of
autonomous work without a review creates more rebase surface area
and delays anything from reaching production. This gets flagged to
Brandon as 🔴 urgent tonight.

## Tonight's plan (written before executing)

1. **Ship `/pilot` landing page** on current main, using the
   sticker-brand design system. Conversion-optimized layout: hero,
   what you get, what we ask, success criteria, privacy, pricing
   after. Deliberately orthogonal to PR #2's old pilot-page
   attempt (different branch state, different design system).
2. **Ship a new K–5 blog post**: "How to teach public speaking in
   elementary school" — targets the explicit SEO phrase from
   `gtm.md`, links to `/pilot` and the 5-minute warm-up post.
3. **Draft new outreach**: enrichment program director at
   independent / private K–5 schools. Peer-to-peer tone, not
   equity-framed, fast-sign lane.
4. **Update `/ceo`**: journal, product-notes, gtm (new post, new
   draft), backlog, metrics, INBOX (escalate #1 to 🔴 and add PR
   backlog as new urgent ask).

## What shipped to `claude/gracious-faraday-ZXSqO`

**New `/pilot` page** — `apps/app/src/app/pilot/page.tsx`:
- Hero: "30 days. One classroom. No cost." — single clear ask.
- "What you get": four-card grid (product, onboarding, day-30
  report, weekly check-ins).
- "What we ask": four-card step-styled grid (one teacher, two
  sessions/week, existing Chromebooks, 20-min debrief).
- "How we define success": three honest numbers (completion rate,
  filler-word delta, teacher confidence) + optional student voice.
- "Private by design": five specific privacy lines, including the
  honest "we don't currently hold SOC 2, COPPA, or FERPA
  certifications." Consistent with the honesty pass.
- "What this pilot is not": four honest exclusions (billing trap,
  surveillance, teacher replacement, speech therapy).
- "After the pilot" pricing tiers with School featured at $3/
  student/year and 50% Title I discount called out.
- CTA band with two routes: `/contact` (conversation) and
  `/blog/five-minute-elementary-speaking-warmup` (soft touch).

**Blog post** — `apps/app/src/app/blog/how-to-teach-public-speaking-elementary-school/page.tsx`:
- 9-minute read. Targets the `gtm.md` SEO phrase "how to teach
  public speaking in elementary school" plus three related queries.
- Argues against quarterly "presentation day" in favor of 5-minute
  daily reps. Includes year-at-a-glance scope/sequence mapped to
  SL.K–SL.5 (fall/winter/spring). Three-part rubric. "What to avoid"
  section calls out over-correcting fillers, public ranking, and
  forcing eye contact — consistent with our K–5 feedback stance.
- Links out to `/pilot` and `/blog/five-minute-elementary-speaking-warmup`
  for SEO internal-linking and conversion.
- Soft, honest self-plug section ("we build Bright Speaker, so
  take this with the appropriate salt").

**Site plumbing:**
- `apps/app/src/app/blog/page.tsx`: new post at top of the index.
- `apps/app/src/app/sitemap.ts`: added `/pilot` at priority 0.9,
  added the two previously-missing blog posts
  (`five-minute-elementary-speaking-warmup` was missing from the
  sitemap before tonight), added the new post.
- `apps/app/src/components/SiteFooter.tsx`: "For schools" column
  now leads with "Free 30-day pilot" → `/pilot`.
- `apps/app/src/app/page.tsx`: hero secondary CTA "Start a free
  pilot" now points to `/pilot` instead of `/dashboard`. Small
  copy fix but materially improves the outreach → homepage → pilot
  flow.

**CEO brain:**
- `ceo/outreach-drafts/2026-04-23-independent-k5-enrichment-director-cold-intro.md`
  — new draft covering the fourth ICP persona. Peer-to-peer, not
  equity-framed; leans into our honest "not certified" line as a
  trust signal. Notes on personalization included.

## What I didn't do (and why)

- **Did not touch PRs #8, #9, #10, or #5.** These are Brandon's to
  merge. Creating a sixth overlapping PR would make it worse. My
  branch is deliberately orthogonal — different files, no
  conflicts on any unchanged route.
- **Did not fix the `speak/page.tsx:304` lint error.** Both PR #8
  and PR #9 already fix this. No need for a third attempt.
- **Did not rewrite `/for-schools` or `/for-educators`.** PR #8 is
  the reconciliation; I shouldn't clobber it.
- **Did not ship a second blog post on "pauses beat um."** PR #9
  has that post; I picked a different SEO target to avoid overlap.
- **Did not enable analytics, wire contact form, or migrate
  localStorage.** All require Brandon decisions / paid services.
- **Did not send any outreach.** Drafts only.

## Verifications

- `pnpm install --ignore-scripts` — clean.
- `npx tsc --noEmit` — clean.
- `pnpm run test` — 2/2 pass (unchanged baseline).
- `pnpm run lint` — 1 pre-existing error on `speak/page.tsx:304`
  (same as nights 2 and 3; fixed in PRs #8 and #9). No new lint
  issues introduced tonight.

## Metrics delta

- Public pages: 8 → 9 (`/pilot`).
- Educator-focused blog posts live on main: 2 → 3 (after this PR
  merges; the five-minute-warmup post is already live).
- Outreach drafts ready: 4 → 5 (speech/debate, ELA, Title I
  principal, literacy coach via PR #9, enrichment director).
- Sitemap entries: 8 → 11.
- Open draft PRs on the repo: 5 → 6 (this one). Notably bad.

## Asks for Brandon (mirrored in INBOX.md and Slack)

- 🔴 **PR review backlog.** Five open draft PRs (#2, #5, #8, #9,
  #10). Nothing has shipped to production since PR #7 on 2026-04-20.
  Proposed order of attention: close #2 (superseded); review/merge
  #8 (smallest surface; design reconciliation + honesty test); then
  #9 (filler profiles + blog post + lint fix overlaps); then #5
  (auth — biggest blast radius, needs real attention); then #10
  (bstack harness). Nightly CEO work compounds only if code merges.
- 🔴 **INBOX #1 still open (now 4 nights).** `brightspeaker.com`
  403s to WebFetch, so the SEO from the `/blog` and new `/pilot`
  pages is compounding into a dark void. If this isn't resolved
  before PR #8 or #9 merges, we're publishing to a site Google
  may not be indexing.
- 🟡 **Review the new enrichment director outreach draft** at
  `ceo/outreach-drafts/2026-04-23-independent-k5-enrichment-director-cold-intro.md`.
  This is our fastest-signing ICP lane; personalize per recipient
  before sending.
- 🟡 **Review the /pilot page** before it goes live. The pricing
  numbers ($3/student/year, 50% Title I off, District "custom")
  are from `strategy.md` / `gtm.md` — if you want different
  numbers committed-in-writing, this is the time to say so.
- 🟢 **FYI — the pilot page repeats the "we don't currently hold
  SOC 2, COPPA, or FERPA certifications" line** in the Privacy
  section. This is consistent with night 2's honesty pass and
  keeps us safe from overclaim, but if you ever want to soften
  that language once the formal work is underway, let me know.
- 🟢 **FYI — outreach pipeline**: five drafts ready, zero sent.
  Pipeline state unchanged from night 2.

## Queued for night 4+

- **Close PR #2** once #8 and #9 land (superseded).
- **Case-study template page** so the first pilot's day-30 report
  can slot into a ready URL. Dry-run on a hypothetical
  "Ms. Patel's 3rd-grade class, Lakewood Elementary" anonymized
  example.
- **FAQ page for pilots** (what does "no cost" mean; what happens
  to data at the end; can we renew; etc.) as a companion to
  `/pilot`.
- **OG image** for sharing `/pilot` on LinkedIn. Currently uses
  the default site OG.
- **A second wave of outreach drafts**: director of elementary
  curriculum at a specific mid-sized district (the expansion
  buyer); and a warm-intro template when Brandon has a named
  mutual connection.
- **Grade-band-tagged prompt expansion** (backlog [P][ux]) — only
  makes sense after PR #9's grade-band work merges.
- **An honesty-as-CI regression test** if PR #9 doesn't merge
  soon, so we don't lose the work.

## What I learned

- The nightly cadence only compounds if main keeps moving. Five
  open PRs means the last two nights of CEO work haven't touched
  production. Flagging this hard tonight and deliberately keeping
  my branch orthogonal so Brandon can merge PRs in any order
  without conflict.
- The Title I principal outreach had nowhere to click through to.
  A pilot page is a small piece of paper that upgrades every
  existing draft's conversion. Should have shipped this night 1.
- The independent / private K–5 enrichment director persona is
  under-served by the site's equity-framed Title I angle. Keeping
  one outreach template dedicated to this audience (non-equity,
  peer-to-peer) is probably the right pattern long-term.

## Notes for future-me

- If PR #8 merges before my branch, the `/for-schools` and
  `/for-educators` design reconciliation work will land on main
  and my `/pilot` page will stay on the same sticker-brand system
  (no conflict).
- If PR #9 merges first, the filler-word grade-banding and the
  `why-pauses-beat-um` blog post land on main. My branch adds a
  non-overlapping blog slug, so no conflict on blog content.
  Sitemap conflict is possible but trivial (both add entries).
- Do NOT duplicate the `honesty.test.ts` regression test that PRs
  #8 and #9 both add. If one of them merges, the test will be
  live on main; my branch doesn't need its own copy.
- Keep paying attention to `/pilot`'s pricing commitments. The
  tiny disclaimer under the pricing grid ("indicative…confirmed
  in writing before any paid conversion") is our insurance until
  Brandon approves the numbers for real.
