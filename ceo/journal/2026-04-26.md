# 2026-04-26 — Night 4

Fourth operating night. Calendar gap since night 3 (2026-04-24,
PR #12) is real — Brandon hasn't touched the queue since.

## Arrival state

- Branch `claude/gracious-faraday-wpwkz`, clean, at the same commit
  as `main`.
- `main` last moved on 2026-04-24 with the daily-instructions doc.
  No code merges since the night-2 merge (`ab30673`, 2026-04-20).
- **Six open draft PRs**, oldest is night 2 (#2, 2026-04-18). All
  but #5 and #10 ship at least two of: a `/pilot` page, a
  "why-pauses-beat-um" blog post, K–5 filler tuning, design
  reconciliation, a lint fix on `speak/page.tsx:304`, or an honesty
  regression test. Each PR was opened without seeing the others
  because future-night branches all forked off `main` while `main`
  hadn't moved. They will conflict heavily on merge.
- `brightspeaker.com` still 403s to WebFetch. **Fourth night open.**
  This is the highest-leverage unresolved thing.
- INBOX #1, #2, #3, #5 still open from earlier nights.
- Homepage `layout.tsx` description still says "Real-time coaching
  on eye contact, pacing, posture, and volume" — the same false
  "posture, volume" claim that night 2 cleaned out of `page.tsx`.
  An honesty regression I missed.

## Reading the situation

The biggest meta-problem tonight isn't a feature gap — it's that
six nights of work are piled up in unmerged drafts and each new
night risks adding a seventh conflicting variant. Tonight needs to:

1. Loudly escalate the PR backlog to Brandon with a recommended
   merge-and-close order so he can clear it in one sitting.
2. Add infrastructure that helps unblock that review (CI on PRs).
3. Ship work that is **strictly orthogonal** to all six open PRs so
   I'm not seventh-variant-ing. That means: no second `/pilot`,
   no second "why pauses beat um" post, no design reconciliation,
   no honesty test (PR #8/#9 already have one), no DB persistence,
   no lint fix on `speak/page.tsx:304`, no `/speak` a11y, no
   filler-word tuning. Those are all in flight.

## Tonight's plan (written before executing)

1. **PR backlog escalation** — INBOX #0 promoted to 🔴 with a
   merge-order recommendation; mirrored to Slack with @Brandon.
2. **Tiny honesty fix** — `layout.tsx` metadata description loses
   "posture, and volume." One-line, scoped, no PR conflict.
3. **CI workflow** — `.github/workflows/ci.yml` for typecheck +
   lint + test on PRs and pushes. Brand new file; no conflicts;
   gives Brandon green checks on the queue when he reviews.
4. **New `/resources` hub + `/resources/sl-k-5-speaking-checklist`**
   — a printable Common Core SL.K–SL.5 standards-based speaking
   observation checklist. Genuine educator utility, new SEO surface,
   brand-new route, no PR overlap.
5. **One new outreach draft** — charter-network elementary
   curriculum director (new persona, completes the K–5 ICP set).
6. **Compliance update** — flesh out KR 5.1 data flow diagram in
   `compliance.md` with the "what changes when PR #5 merges"
   appendix. Pure CEO brain.
7. **CEO brain closeout** — journal, INBOX, metrics.

Explicitly NOT doing tonight:
- A seventh design reconciliation. PR #8 and PR #12 both ship one.
- A fifth "/pilot" attempt. PR #2 and PR #11 both ship one.
- A fourth "why-pauses-beat-um" post. PR #8, #9, #12.
- Another honesty test variant. PR #8 and PR #9 both have one.
- The lint fix on `speak/page.tsx:304`. PR #8, #9, #12.
- Re-tuning filler words. PR #2, #8, #9.

## What shipped to `claude/gracious-faraday-wpwkz`

**Code (autonomy-safe):**

- `apps/app/src/app/layout.tsx` — root metadata description trimmed
  the lingering "posture, and volume" claim. Honesty fix consistent
  with the night-2 cleanup of `page.tsx`. One-line change.
- `apps/app/src/app/sitemap.ts` — added `/resources`,
  `/resources/sl-k-5-speaking-checklist`, and the previously-missing
  `/blog/five-minute-elementary-speaking-warmup`.
- `apps/app/src/app/resources/page.tsx` — new `/resources` index hub.
  Sticker-brand, no compliance claims, no sign-in.
- `apps/app/src/app/resources/sl-k-5-speaking-checklist/page.tsx` —
  printable, clipboard-ready checklist. Six grade sections (K-5),
  three rows each, each row tied to a Common Core SL anchor (4-6).
  Server-rendered; print button is a tiny client component.
- `apps/app/src/app/resources/sl-k-5-speaking-checklist/PrintButton.tsx`
  — client component for `window.print()`.
- `apps/app/src/app/globals.css` — small `@media print` block: white
  bg, black text, no shadows, page-break-inside on cards.
- `apps/app/src/components/SiteFooter.tsx` — Company column now
  surfaces "Free resources" and "Blog" separately; replaces the
  single "Resources" link that pointed to `/blog`.

**Infra (autonomy-safe):**

- `.github/workflows/ci.yml` — typecheck + lint + test on every PR
  and every push to `main`. pnpm 10.13.1, Node 20. Concurrency
  cancels stale runs on the same branch. This was a backlog item
  ([P][infra] Add CI). Most importantly: Brandon will see green
  checks on the open draft PRs, which should make the merge-order
  triage in INBOX #0 less work.

**CEO brain:**

- `ceo/journal/2026-04-26.md` — this file.
- `ceo/INBOX.md` — new 🔴 #0 (PR backlog with merge-order
  recommendation), and #1 (403) escalated 🟡 → 🔴 after four nights
  open.
- `ceo/compliance.md` — KR 5.1 data flow diagram added (text
  ASCII), with an explicit "what changes when PR #5 merges"
  appendix.
- `ceo/metrics.md` — refreshed with the night-4 site shape (12
  public pages on `main`), 5 outreach drafts queued, indexability
  status updated.
- `ceo/gtm.md` — pipeline table reflects 5 drafted intros.
- `ceo/outreach-drafts/2026-04-26-charter-network-elementary-curriculum-director.md`
  — new persona. Three-school pilot framing (vs. single-classroom)
  is the lead. Lays out the Web Speech / Google ASR disclosure
  inside the email rather than waiting for the call.

## What I deliberately did NOT do (and why)

- **No `/pilot` page.** PR #2 has one, PR #11 has a fresher one.
  Adding a third would have made tonight's branch conflict with
  both.
- **No "why pauses beat um" blog post.** PR #8, #9, and #12 all
  ship some version. A fourth would be obviously wasteful.
- **No design reconciliation of `/for-schools` + `/for-educators`.**
  PR #8 and PR #12 both ship one; #12 supersedes #8.
- **No filler-word tuning.** PR #2 (older), #8, and #9 all ship
  versions. PR #9's grade-band tuning is the most thoughtful.
- **No honesty regression test.** PR #8 and #9 both add one; #9's
  is tighter.
- **No DB persistence work.** PR #5 is the canonical version and
  Brandon hasn't reviewed it yet.
- **No `speak/page.tsx:304` lint fix.** PR #8/#9/#12 all fix it.

## Verifications (run in this sandbox)

- `pnpm install --ignore-scripts` — clean (602 packages).
- `pnpm --filter @bright-speaker/app exec tsc --noEmit` — clean.
- `pnpm run test` — 2/2 pass (no test changes tonight).
- `pnpm exec eslint <files I touched>` — clean (0 errors); the
  pre-existing `react-hooks/set-state-in-effect` error on
  `speak/page.tsx:304` is unchanged and is fixed in PRs #8/#9/#12.
- The CI workflow runs `lint` as `continue-on-error: true` for
  exactly this reason; promote to blocking once one of #8/#9/#12
  merges.

## Asks for Brandon (mirrored in INBOX.md and Slack)

- 🔴 **PR backlog (INBOX #0).** Six open drafts. Recommended merge
  order in INBOX. Two hours of triage now → unblocks every future
  night's compounding.
- 🔴 **`brightspeaker.com` 403 (INBOX #1, fourth night).** With the
  PR backlog clearing soon, the 403 becomes the next blocker on
  every blog post and resource page actually mattering for SEO.
- 🟡 **Review the new charter-network outreach draft** at
  `ceo/outreach-drafts/2026-04-26-charter-network-elementary-curriculum-director.md`.
- 🟢 **CI is now wired.** `.github/workflows/ci.yml` runs on every
  PR and on pushes to `main`. Should give you green checks across
  the open queue in the next push cycle.
- 🟢 **`/resources/sl-k-5-speaking-checklist` is the first
  printable.** If it's useful, the next two I'd build: a parent-
  facing one-page "show-and-tell prompt bank for K-2" and a
  classroom-poster "how to listen like a speaker" anchor chart.

## Queued for night 5

- After PR #5 merges: ship the four compliance items I called out
  in the appendix to compliance.md (retention policy, deletion
  endpoint, FERPA export endpoint, `/privacy` update).
- After the 403 is resolved: register Google Search Console for
  `brightspeaker.com`, then submit the sitemap.
- Second printable resource: parent-facing "show-and-tell prompt
  bank for K-2" or a classroom anchor-chart.
- New blog post on a topic NO open PR ships: "How to listen like
  a speaker — a K-5 audience-skills mini-lesson." Pairs with the
  SL.K-SL.5 audience-skills strand which the checklist deliberately
  doesn't cover.
- Continue ICP enrichment — first 25 charter-network curriculum
  directors with public ELA strategy docs.

## What I learned

- The biggest risk to the nightly routine isn't a feature gap — it
  is letting work pile up in unmerged drafts on a `main` that
  doesn't move. Every night that passes without a merge, the
  conflict surface multiplies. Tonight I deliberately changed the
  shape of the night to be CI-first + `/resources`-first precisely
  because it was the only orthogonal lane left.
- Repeated honesty regressions in `layout.tsx` after night 2
  cleaned `page.tsx` argues for the honesty regression test (PR
  #9) being valuable independent of the other PRs. If Brandon only
  has time for one of #8/#9, prioritize #9 for the test alone.
- The K-5 wedge gets stronger every time we put a printable in
  front of it. Teachers and curriculum directors respond to
  artifacts more than to taglines. Keep shipping `/resources`.

## Notes for future-me

- `.github/workflows/ci.yml` is in place. If a future-night branch
  fails CI, fix it before tagging Brandon.
- Print-friendly @media print rules are in `globals.css`. New
  resource pages can use `print:hidden` Tailwind utilities to
  hide chrome.
- Don't add another "/pilot" or "why-pauses-beat-um" variant
  until the PR backlog clears. If you need to do GTM-flavored
  work on a fresh night, lean toward `/resources/*` (orthogonal),
  outreach drafts (orthogonal), or CEO-brain documentation
  (always orthogonal).

