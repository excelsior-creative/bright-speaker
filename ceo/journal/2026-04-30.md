# 2026-04-30

## Browser demo access + speaking interface improvement

- Source: Brandon Slack request in `#cb-brightspeaker` to make the homepage/app homepage route directly into a try-it-now browser demo and improve the speaking interface design.
- Risk: R2, aligned with `ceo/specs/2026-04-27-browser-support-speak-fallback.md`; no new vendor, dependency, auth, storage, analytics, deployment, or student-data persistence changes.
- Changes made on branch `feat/browser-demo-speaking-interface`:
  - Homepage hero, footer CTA, pricing CTA, and site nav now prioritize direct `/speak?prompt=1` browser-demo entry while keeping demo-booking as secondary.
  - App dashboard quick-start copy now frames the flow as an instant browser demo.
  - `/speak` prep screen redesigned into a stronger two-column demo interface with prompt, coaching tips, permission expectations, and clearer start CTA.
  - `/speak` recording screen improved with a wider camera area, visible runtime speech fallback alert, richer prompt/transcript panel, and coach checklist.
  - Added speech-recognition support helper/tests and visible runtime speech-recognition error mapping.
  - Added `ceo/browser-support.md` with the current support matrix and risk caveats.
- Verification:
  - `pnpm test` passes (2 files, 7 tests).
  - `pnpm lint` passes with one pre-existing/custom-font warning in `apps/app/src/app/layout.tsx`.
  - `pnpm --filter @bright-speaker/app build` passes.
  - `git diff --check` passes.
  - Local browser smoke on `localhost:3111`: homepage renders direct "Try the browser demo" CTAs; `/speak?prompt=1` renders redesigned prep UI; clicking start shows in-page camera fallback in the headless/no-camera environment instead of proceeding silently.
