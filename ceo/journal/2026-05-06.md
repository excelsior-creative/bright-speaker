# 2026-05-06

## Timmy — Functional MVP completion plan

Brandon asked in Slack `C0AD94TPCLQ` for a real plan to finish BrightSpeaker into a functional MVP.

Actions:
- Confirmed Slack channel context maps to BrightSpeaker from prior BOS/session routing evidence.
- Created `ceo/specs/2026-05-06-functional-mvp-completion-plan.md`.
- Updated `ceo/next.md` to point to the MVP plan as the granular implementation sequence.
- Added an `INBOX.md` ask for explicit R4 implementation approval.

Current MVP target:
- Teacher signs in.
- Teacher creates class and gets class code.
- Student joins by class code without adult-style account creation.
- Student completes browser-first speaking practice.
- Privacy-minimized session summary persists server-side.
- Teacher sees class progress.
- Teacher can copy/export a principal-ready pilot summary.

Risk gate:
- Effective tier is R4 before implementation/deployment because MVP touches auth, class membership, server-side student-session records, and children/student data.
- Planning/docs are safe; product implementation should not start until Brandon explicitly approves the R4 scope.

Verification:
- Read current `origin/main` via clean worktree `/tmp/bright-speaker-mvp-plan`.
- Checked open PRs: #21 and #5 are both conflicting; #5 should be treated as reference, not merged directly.
- Attempted baseline `pnpm test`; blocked because worktree has no `node_modules` / `turbo` installed. Next implementation pass should run `pnpm install` first, then `pnpm test`, `pnpm lint`, `pnpm build`.
