# 2026-04-17 — Night 1

**First night of the nightly CEO routine.** Arrived to an
unscaffolded `/ceo` folder. Spent the first third of the night
reading the repo and establishing persistent memory.

## Arrival state

- Repo on branch `claude/exciting-dijkstra-MS4VE`, clean.
- Most recent commits are product-focused: level-up celebration,
  `/practice → /speak` redirect, webcam stream fix, brand identity
  rollout.
- Live site (brightspeaker.com) returned 403 to WebFetch. Could be
  Vercel deployment protection or a Cloudflare WAF setting. Flagged
  to Brandon as 🟡 in INBOX.
- No `/ceo` folder. No educator-facing pages. Footer links
  (`Privacy`, `Terms`, `Contact`) all `href="#"`. "Watch Demo"
  button did nothing. No `robots.ts`, no `sitemap.ts`, no OG tags.
- Stack is healthy: Next.js 16.1.6 + React 19 + Tailwind v4, Clerk
  installed (not gating), Neon in deps (not wired), `face-api.js`
  for eye-contact proxy, `vitest` tests green.
- Session data lives in browser `localStorage`. Not per-user. Not
  in a DB. Will need to migrate when auth goes live.
- "AI feedback" today is a rule-based `if/else` ladder. No Claude
  call happens, despite the PRD mentioning one. Documented in
  `product-notes.md`.

## Tonight's plan (written before executing)

1. Scaffold `/ceo` with the full structure the mandate calls for.
2. Ship educator-facing pages that GTM needs: `/for-schools`,
   `/for-educators`, `/privacy`, `/terms`, `/contact`, plus a
   resources/blog and one post.
3. Fix broken footer links + remove the dead "Watch Demo" button.
4. Add `robots.ts`, `sitemap.ts`, `metadataBase`, OG + Twitter.
5. Draft 2 cold-outreach emails and a pilot-program one-pager.
6. Slack summary + INBOX updates.

## What shipped to `main`-bound branch

**Code:**
- `src/components/SiteNav.tsx` + `src/components/SiteFooter.tsx` —
  shared site chrome that replaces the ad-hoc nav/footer on the
  landing page.
- `src/app/for-educators/page.tsx` — teacher + coach value prop,
  weekly cadence, audience-specific sections, pilot CTA.
- `src/app/for-schools/page.tsx` — school/district positioning,
  three-step adoption path (classroom → building → district),
  data-handling section, FAQ, pilot CTA.
- `src/app/privacy/page.tsx` — plain-English data-flow explainer.
  Explicitly flagged as non-legal; no compliance claims made.
- `src/app/terms/page.tsx` — short plain-language terms while we
  wait for the formal version.
- `src/app/contact/page.tsx` — mailto-backed contact stubs
  (`hello@brightspeaker.com`). Brandon needs to make sure that
  inbox is routed.
- `src/app/blog/page.tsx` + `src/app/blog/reduce-filler-words-k12/page.tsx`
  — blog index and first educator post (~1500 words).
- `src/app/robots.ts`, `src/app/sitemap.ts` — SEO basics. Disallow
  crawlers on the authed product surfaces (`/dashboard`, `/speak`,
  `/history`).
- `src/app/layout.tsx` — `metadataBase`, OG, Twitter card,
  keyword list.
- `src/app/page.tsx` — rewritten hero with educator-first framing,
  dropped the broken "Watch Demo" button, added audience-split
  cards for `/for-educators` and `/for-schools`, swapped the ad-hoc
  nav + footer for the shared components.

**CEO brain (`/ceo/`):**
- `README.md`, `strategy.md`, `okrs.md`, `roadmap.md`,
  `backlog.md`, `product-notes.md`, `gtm.md`, `compliance.md`,
  `metrics.md`, `competitive.md`, `experiments.md`, `INBOX.md`.
- `decisions/0001-educator-wedge-over-direct-to-consumer.md`.
- `outreach-drafts/2026-04-17-speech-debate-coach-cold-intro.md`.
- `outreach-drafts/2026-04-17-ela-teacher-cold-intro.md`.
- `outreach-drafts/2026-04-17-pilot-program-one-pager.md`.

## What I didn't do (and why)

- **Did not change auth, session handling, or the data model.**
  Those require a PR per the mandate. Captured in `backlog.md`.
- **Did not touch AI-feedback scoring or add an LLM call.** Same
  reason — those are PR-only, and they need teacher review before
  anything kid-facing.
- **Did not publish a formal privacy policy or ToS.** Those are
  Brandon-approval-required. Shipped a plain-English data-flow
  page that makes no compliance claims.
- **Did not send any outreach.** Drafts only; Brandon sends.
- **Did not spin up new third-party services** (analytics, Sentry,
  etc.) — paid/sub-processor decisions are Brandon's.

## Verifications

- `npx tsc --noEmit` → clean.
- `npm run lint` → clean.
- `npm run test` → 2/2 pass (session reward tests unchanged).
- `npm run build` → fails in this sandbox because the sandbox
  can't reach `fonts.googleapis.com`; this is an environment
  limitation, not a code regression. The font imports are
  pre-existing.

## Metrics delta

- Public pages: 2 → 8.
- Educator-focused resources: 0 → 1 blog post.
- Broken footer links: 3 → 0.
- Outreach drafts ready for Brandon to review and send: 0 → 2 +
  a one-pager.

## Asks for Brandon (mirrored in INBOX.md and Slack)

- 🟡 **Is brightspeaker.com reachable by crawlers?** WebFetch got
  a 403. Need to know if we're indexable or gated.
- 🟡 **Review the three outreach drafts** in
  `ceo/outreach-drafts/` and either send or give me feedback.
- 🟢 **Analytics decision** — we have zero server-side telemetry.
  Proposal: Plausible or self-hosted PostHog. Sub-processor, so
  your call.
- 🟢 **FYI**: `/privacy` and `/terms` shipped as plain-English
  stubs with no compliance claims. Formal policy drafting is
  queued for when you're ready.
- 🟡 **Confirm `hello@brightspeaker.com` is routed.** The contact
  page and outreach drafts use it. If it bounces, we look amateur.

## Queued for night 2

- Accessibility audit: focus styles, skip link, `aria-live` for
  live counters in `/speak`.
- Bundle / perf pass on `/speak` (face-api.js weights are loaded
  every visit).
- Second blog post: "A 10-minute weekly speaking drill for ELA
  classrooms" — expand the cadence from tonight's post into a
  printable classroom handout.
- Start ICP-list compilation (NSDA coach directory, state speech
  associations).
- Audit the filler-word list; drop `so`, `well`, `yeah` for
  younger grades.

## What I learned

- The tension between the product's warm parent-flavored framing
  and the school-licensing GTM was sharper than I expected.
  Captured as decision `0001-educator-wedge-over-direct-to-
  consumer.md`. Kept the warmth; shifted the direction.
- The "AI feedback" story on the product is currently aspirational
  — it's all rule-based. Any marketing language that says "AI"
  needs to be concrete ("live filler-word detection") rather than
  implying an LLM loop that doesn't exist yet.
- The honest privacy page is probably a competitive advantage in
  educator conversations. Wrote it without hedging.

## Notes for future-me

- Keep `/ceo/product-notes.md` in sync whenever the product
  materially changes. Future nights will burn time re-reading the
  repo otherwise.
- The 403 from brightspeaker.com is the highest-leverage thing to
  resolve — if the site isn't indexable, the blog post and SEO
  work don't compound.
